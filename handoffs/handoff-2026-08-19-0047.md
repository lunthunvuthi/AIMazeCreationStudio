# Session Handoff - 2026-08-19 (0047)

## Goal
Three things this session, all stemming from the designer's print-template sample
arriving: (1) understand `pickaxe-maze-creation/Images/Sample/Kinder July-week4-01.pdf`
and write a PDF export spec from it; (2) turn a follow-up description of a Level
Dashboard redesign (page-rows instead of a flat question grid) into a written plan;
(3) clean up the repo layout (docs/ subfolders, wrap `pickaxe-maze-creation` in a
`Maze-All-Contents` container) and fix every reference to the moved paths.

## Status snapshot
All three are done. Two spec docs written (no app code changed), the folder reorg is
complete and verified (tests pass, both dev servers back up), everything is committed
and pushed to `origin/main` at `39368bc`. Working tree clean.

## Done
1. **PDF export spec** — read the designer sample (`Kinder July-week4-01.pdf` + its
   answer-key counterpart), confirmed via PDF structure inspection that both are
   flattened A4 raster images (2480×3508px, no vector text/fonts — visual reference
   only, nothing extractable). Wrote `Web App/docs/pdf_export_spec.md`: page geometry,
   document structure (cover → question pages → bonus page), per-panel layout (pickaxe
   badge, grid, Start/Goal icons), answer-key variant, and an open-decisions list.
   Cross-referenced from `Web App/docs/development_plan.md` §1/§7/§9/§10.
   - **Correction mid-session:** initially flagged the "Think! Think!" logo and mascot
     cat as third-party IP not to reproduce. The user corrected this — it's the
     company's own branding ("Hatenyan") — safe to use directly. Fixed in the spec (§0)
     and in memory.
2. **Level Dashboard page-row redesign plan** — user described replacing the 3-column
   question grid with a list of page-rows (1-2 questions per row, mapping 1:1 to a PDF
   page), a locked cover/tutorial row, drag-and-drop reorder/swap, empty-row
   self-deletion, and new Preview/Download(PDF+JSON) actions. Wrote
   `Web App/docs/level_dashboard_pagination_spec.md` covering the data-model change
   (`LevelProgress.questions[]` → `pages: PageRow[]`, `formatVersion` 1→2 + migration),
   dashboard layout, drag-and-drop mechanics (3 gestures), and Preview/Download gating.
   Three forks were confirmed via `AskUserQuestion` (see Decisions below) before writing.
3. **Repo reorg** (requested after the two spec docs existed):
   - `pickaxe-maze-creation/difficulty_setting.md`, `generation_spec.md`, `rules.md`,
     `validator_design.md` → moved into a new `pickaxe-maze-creation/docs/` folder.
     `README.md` deliberately stayed at the package root (folder entry point).
   - `Web App/development_plan.md`, `pdf_export_spec.md`,
     `level_dashboard_pagination_spec.md` → moved into a new `Web App/docs/` folder.
     `Web App/frontend/` and `Web App/backend/` were not touched.
   - `pickaxe-maze-creation/` (whole folder) → moved into a new top-level
     `Maze-All-Contents/` container: `Maze-All-Contents/pickaxe-maze-creation/`.
   - Fixed every reference to the old paths: root `pyproject.toml`
     (`package-dir`, `testpaths`), root `.gitignore`, root `README.md`, the moved docs'
     cross-references, and the literal `../*.md` docstring paths inside
     `pickaxe_maze/*.py` (now `../docs/*.md`). `handoffs/*.md` were deliberately left
     describing the old paths (timestamped snapshots, not living docs).
   - **Verified, not just moved:** reinstalled the editable package
     (`pip install -e ".[dev]"` — its finder hardcodes an absolute path to
     `pickaxe_maze/`, which had gone stale), confirmed `import pickaxe_maze` resolves to
     the new location, reran the full suite (**32 passed**). Restarted the backend
     uvicorn dev server (was bound to the stale path in memory); left the frontend vite
     server untouched since nothing under `Web App/frontend/` moved.
4. **Committed and pushed**: commit `39368bc` "Reorganize docs into docs/ subfolders,
   move pickaxe-maze-creation under Maze-All-Contents/" on `main`, pushed to
   `git@github-personal:lunthunvuthi/AIMazeCreationStudio.git`. Includes the reorg +
   both new spec docs + the previously-untracked `handoffs/handoff-2026-08-18-2321.md`
   from the prior session (swept in, same as that session swept in the one before it).

## Next step
Nothing queued by the user. If picking this back up, the natural next move is starting
**implementation** of one of the two specs — most logically the Level Dashboard redesign
(`Web App/docs/level_dashboard_pagination_spec.md`), since its Phase A (data model +
UI + drag-and-drop + interim in-app preview) doesn't block on the PDF renderer-tech
decision the way `pdf_export_spec.md`'s real renderer does.

## Decisions & conventions
- Confirmed via `AskUserQuestion` this session (don't re-ask):
  - PDF export: the Think!Think!/Hatenyan branding is safe to use directly (not
    third-party IP — this was a correction, see Done §1).
  - Dashboard: cover/tutorial row is **fully locked** — no drag in or out, ever.
  - Dashboard: Download is **gated on a fresh Preview** since the last edit — recommended
    mechanism is comparing a `JSON.stringify(current)` snapshot, not a manually-toggled
    dirty flag (avoids the same "heuristic, not guaranteed" risk already flagged for
    `question_id` occurrence-counting in the prior handoff).
  - Dashboard: the in-row "+ Add question" **keeps today's star-picker dropdown**; only
    the new "+ Add new page" button defaults straight to 3-star.
- Repo reorg conventions set this session: each package gets its own `docs/` subfolder
  for specs; each package's top-level `README.md` stays put as the entry point, not
  moved into `docs/`; only `pickaxe-maze-creation` got wrapped in `Maze-All-Contents/`
  (Web App was not moved anywhere).
- `[[feedback_spec_conflicts]]` (standing memory: ask before editing a design doc's
  numeric constants): not triggered this session — no numeric constants in
  `difficulty_setting.md` were touched, only doc-location cross-references.

## Gotchas / watch-outs
- The editable pip install (`pip install -e .`) hardcodes an **absolute filesystem path**
  to `pickaxe_maze/` in `.venv/lib/python3.14/site-packages/__editable___maze_studio_0_1_0_finder.py`.
  Any future move of that package requires a `pip install -e ".[dev]"` reinstall, or
  `import pickaxe_maze` will fail. This bit us this session; now documented in memory
  (`project_folder_reorg`).
- Two renderers/decisions are still genuinely unresolved (not this-session oversights,
  tracked in `pdf_export_spec.md` §7): the actual PDF-generation tech (backend
  reportlab/weasyprint vs. frontend browser-print) and the icon set for Start/Goal in
  print vs. the in-app letter renderer. Neither blocks writing more spec, but both block
  real rendering code.
- No drag-and-drop library exists in `Web App/frontend/package.json` yet — the dashboard
  spec recommends `@dnd-kit/core`, not yet added or confirmed.
- Dev servers: backend uvicorn restarted this session (new PID, port 8000, confirmed
  responding); frontend vite (port 5173) has been running unrestarted since at least the
  prior session — untouched by this session's changes, no reason to restart it.

## Key files & locations
- `Web App/docs/pdf_export_spec.md` — PDF print template spec (from the designer
  sample). §7 has the open decisions.
- `Web App/docs/level_dashboard_pagination_spec.md` — Level Dashboard page-row redesign
  plan. §7 has files likely touched during implementation; §8 has open decisions.
- `Web App/docs/development_plan.md` — original app spec, now cross-referencing both
  docs above from §6.5, §7, §9, §10.
- `Maze-All-Contents/pickaxe-maze-creation/` — the generator/validator package (moved
  this session), with `docs/` holding `difficulty_setting.md`, `generation_spec.md`,
  `rules.md`, `validator_design.md`.
- `Maze-All-Contents/pickaxe-maze-creation/Images/Sample/` — the two designer sample
  PDFs (`Kinder July-week4-01.pdf`, `Answer Kinder July-week4-01 2.pdf`).
- Memory files written/updated this session (auto-memory, not in-repo):
  `project_pdf_export_spec.md`, `project_level_dashboard_pagination.md`,
  `project_folder_reorg.md` — the latter has the full old-path → new-path mapping if a
  future session finds a stale path reference anywhere.
- Run everything (paths updated for the reorg):
  ```bash
  source .venv/bin/activate && pytest                       # 32 backend/CLI tests
  uvicorn maze_api.main:app --reload --app-dir "Web App/backend" --port 8000
  cd "Web App/frontend" && npm run dev                       # http://localhost:5173
  cd "Web App/frontend" && npx tsc -b && npx oxlint .        # typecheck + lint
  ```
- Repo remote: `git@github-personal:lunthunvuthi/AIMazeCreationStudio.git`, branch
  `main`, now at `39368bc`.

## Open questions
- Which spec to implement first (Dashboard redesign vs. PDF renderer) — no preference
  stated yet; Dashboard Phase A is more self-contained (§8 of its spec).
- Renderer tech (backend vs. frontend) — `pdf_export_spec.md` §7 item 5, unresolved.
- Carried from prior handoffs, still unresolved and untouched this session: whether
  `wf-thunvuthi-lun` has accepted the GitHub collaborator invite, and whether
  `AIMazeCreationStudio` should be made private.

## Resume Prompt
```
Read handoffs/handoff-2026-08-19-0047.md to get up to speed. Last session (1) wrote
Web App/docs/pdf_export_spec.md from the designer's sample PDF, (2) wrote
Web App/docs/level_dashboard_pagination_spec.md — a plan to replace the Level
Dashboard's 3-column question grid with page-rows (1-2 questions/row, drag-and-drop,
locked cover row, Preview/Download actions) — and (3) reorganized the repo: docs/
subfolders under both Web App/ and pickaxe-maze-creation/, and pickaxe-maze-creation
moved to Maze-All-Contents/pickaxe-maze-creation/. All references were fixed
(pyproject.toml, .gitignore, READMEs, docstrings), the editable pip install was
refreshed, the full test suite passes (32 tests), and both dev servers are up. Everything
is committed and pushed as 39368bc on main. No app code has been written yet for either
spec — next step is implementing one of them, most likely the Dashboard redesign since
it doesn't block on the PDF renderer-tech decision. Two unrelated items are still open
from prior sessions: GitHub collaborator invite acceptance and whether to make the repo
private.
```
