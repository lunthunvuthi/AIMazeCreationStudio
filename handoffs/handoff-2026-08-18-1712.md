# Session Handoff - 2026-08-18 (1712)

## Goal
Repo housekeeping carried over from `handoff_20260818_02.md`'s open items: git-init the
project, rename `PickAxe Maze Creation/` to drop spaces, decide `validate`'s report output
location, then get the repo onto a remote with both of the user's GitHub accounts able to
push. No `development_plan.md` roadmap/app-code work this session.

## Status snapshot
All housekeeping done and pushed. Repo is live at
`git@github-personal:lunthunvuthi/AIMazeCreationStudio.git` (public), branch `main` tracking
`origin/main`, working tree clean. A collaborator invite for the user's work GitHub account
is outstanding (not yet confirmed accepted). No app code (frontend/backend) touched — dev
servers left running untouched throughout.

## Done
1. **Decisions gathered via AskUserQuestion** (repo housekeeping, from three named options):
   git init now, rename to `pickaxe-maze-creation` (kebab-case), and `validate` should
   default to writing its report next to the input file instead of always to `output/`.
2. **Renamed `PickAxe Maze Creation/` → `pickaxe-maze-creation/`.**
   - Updated the only functional references: `pyproject.toml`'s `package-dir` and
     `testpaths`.
   - Updated comment-only references in `README.md`, `Web App/development_plan.md`, and
     four frontend files (`types/maze.ts`, `registry/mazeTypes.ts`,
     `registry/pickaxe/starParams.ts`, `registry/pickaxe/wizardMaze.ts`) plus the
     `PROJECT_DIR` comment in `pickaxe_maze/cli.py`.
   - Reinstalled the editable package (`pip install -e ".[dev]"`) so the setuptools
     editable finder re-resolves to the new path — required because it hardcodes an
     absolute path baked in at install time.
   - Verified: `pytest` (32/32 passed), `npx tsc -b`, `npx oxlint .` all clean. Both
     already-running dev servers (uvicorn :8000, vite :5173) kept working with no restart —
     neither watches the renamed directory.
3. **Changed `validate`'s default output location** (`pickaxe-maze-creation/pickaxe_maze/cli.py`,
   `cmd_validate`): `out_dir = args.out_dir or args.path.parent` (was
   `args.out_dir or DEFAULT_OUTPUT_DIR`). Updated the module docstring and the `--out-dir`
   help string to match. Existing tests in `test_cli.py` were unaffected — they always pass
   `--out-dir` explicitly. Verified live: `pickaxe-maze validate
   pickaxe-maze-creation/input/star3-show.json` wrote the report next to the input, then the
   test artifact was deleted.
4. **`git init`** at the repo root, default branch renamed `master` → `main`. Scanned for
   likely secret files (`.env`, `*credential*`, `*secret*`, `*.pem`, `*.key`) before
   staging — none found. Staged by explicit path (not `git add -A`), verified no
   gitignored dirs (`node_modules`, `.venv`, `egg-info`, `__pycache__`) slipped in.
   - Commit 1 (`fdd67c1`, "Initial commit"): 86 files.
   - **Follow-up, user-approved**: noticed the initial commit had swept in ad-hoc
     CLI-generated files under `pickaxe-maze-creation/input/` and `output/` (timestamped
     maze JSONs + `_report.md` files) alongside their `.gitkeep` placeholders. Added
     ignore rules (`pickaxe-maze-creation/{input,output}/*` with `!.../.gitkeep`
     exceptions) and `git rm --cached` (not `rm`) so the files stay on disk — one of them,
     `output/kinder_20260817_184906.json`, was open in the user's editor at the time.
   - Commit 2 (`b62a15e`, "Stop tracking generated maze input/output artifacts").
5. **Set up the remote and pushed**, working around a multi-account gotcha:
   - User created an empty GitHub repo at `lunthunvuthi/AIMazeCreationStudio` and gave the
     SSH URL `git@github.com:lunthunvuthi/AIMazeCreationStudio.git`.
   - First push attempt failed: permission denied, because this machine's default
     `github.com` SSH host is deliberately pinned (per the machine's own `~/.ssh/config`
     comments) to the **work** key (`id_ed25519_wonderfy`, account `wf-thunvuthi-lun`),
     which has no access to `lunthunvuthi`'s personal repo.
   - Fix: this machine already has a `github-personal` SSH host alias
     (`~/.ssh/config`) bound to `id_ed25519_personal`. Verified with
     `ssh -T github-personal` → authenticates as `lunthunvuthi`. Reset the remote URL to
     `git@github-personal:lunthunvuthi/AIMazeCreationStudio.git` and pushed successfully;
     `main` now tracks `origin/main`.
6. **Invited `wf-thunvuthi-lun` as a collaborator** on `lunthunvuthi/AIMazeCreationStudio`
   with `write` (push) permission, at the user's request, so both of their GitHub accounts
   can commit/push to this repo:
   - Temporarily ran `gh auth switch --hostname github.com --user lunthunvuthi` (repo
     owner) to send the invite via `gh api -X PUT
     repos/lunthunvuthi/AIMazeCreationStudio/collaborators/wf-thunvuthi-lun -f
     permission=push` — confirmed success via the API response (invitation id
     `329536846`, `"permissions":"write"`).
   - Switched the active `gh` account back to `wf-thunvuthi-lun` immediately after, so the
     machine's default identity is unchanged from before this session.
   - Flagged to the user (not something this session changed): the repo came back from
     the GitHub API as `"private":false` — it's public.

## Next step
Nothing queued. Two things explicitly surfaced to the user as options, not yet acted on:
1. **Confirm the collaborator invite was accepted** — from the `wf-thunvuthi-lun` GitHub
   account, accept the pending invite (e.g. at
   github.com/lunthunvuthi/AIMazeCreationStudio/invitations, or `gh api
   /user/repository_invitations` while authenticated as `wf-thunvuthi-lun`), then verify a
   push works from that identity using the `github-wonderfy` SSH alias:
   `git@github-wonderfy:lunthunvuthi/AIMazeCreationStudio.git`. This session could not
   verify acceptance — the invite-listing API call needs admin rights on the repo, which
   the active account (`wf-thunvuthi-lun`, a collaborator, not the owner) doesn't have.
2. Decide whether `AIMazeCreationStudio` should be made **private** — it is currently
   public, which the user has been told but hasn't weighed in on.

Beyond this session's housekeeping, the user asked what's next on the actual project and
was given (not yet chosen from):
- Re-confirm the **~15-19s worst-case Randomize wait** (star 8) — cheapest, just a decision,
  carried unanswered since `handoff_20260818_01.md`.
- Build `development_plan.md` §6.4 — auto-generate the 1★ tutorial slot on level creation.
- Roadmap Phase 2 — `localStorage` autosave (next natural step after step 4's file-based
  save/load).
- Roadmap Phase 3 — backend-persisted accounts (bigger lift, no pressure).
- Step 8 — real PDF export, still blocked on a designer providing a print template.

## Decisions & conventions
- **Folder name is now `pickaxe-maze-creation/` (kebab-case), not `PickAxe Maze Creation/`.**
  User chose this explicitly over a PascalCase alternative in an AskUserQuestion.
- **`pickaxe-maze validate` defaults its report next to the input file**, not to `output/`.
  Explicit user choice over keeping the old `output/`-always behavior.
- **Generated CLI output (`pickaxe-maze-creation/input/*`, `output/*`) is gitignored**,
  only `.gitkeep` placeholders are tracked. User-approved follow-up after noticing the
  initial commit swept in scratch files.
- **This repo's remote must always use the `github-personal` SSH alias**, never bare
  `github.com` — the machine's `~/.ssh/config` deliberately points bare `github.com` at the
  work key so an un-aliased remote fails loudly instead of silently pushing as the wrong
  account. Mentioned explicitly to the user as a footgun to remember.
- `[[feedback_spec_conflicts]]` (existing memory): no `development_plan.md` numeric
  constants were touched this session — not applicable, but still standing guidance for
  future sessions.

## Gotchas / watch-outs
- **Two `gh` accounts are logged in on this machine**: `wf-thunvuthi-lun` (work, normally
  active) and `lunthunvuthi` (personal, owns this repo). `gh auth switch` changes global
  CLI state, not per-repo — always switch back to `wf-thunvuthi-lun` after any
  `lunthunvuthi`-scoped `gh api`/`gh` action, as done this session.
- **SSH host aliases, not bare `github.com`, select the identity for git operations**:
  `github-personal` → `lunthunvuthi`, `github-wonderfy` → `wf-thunvuthi-lun`, bare
  `github.com` → work key (deliberately, to fail loudly). Any future remote URL for a
  personal-owned repo needs the `github-personal` alias substituted for `github.com`.
- Collaborator invitations require **acceptance by the invitee**, not just the API call
  succeeding — `write` access won't actually work until `wf-thunvuthi-lun` accepts.
- Dev servers were **left running throughout, never restarted**: uvicorn on :8000
  (`--app-dir "Web App/backend"`), vite on :5173, per the same convention as prior
  sessions — verify they're still up before assuming `localhost` URLs work.
- Still **no test runner beyond `pytest`/`tsc`/`oxlint`** — no CI configured yet in the new
  git repo (out of scope for this session, not asked for).

## Key files & locations
- `pyproject.toml` — `package-dir`/`testpaths` now point at `pickaxe-maze-creation/`.
- `pickaxe-maze-creation/pickaxe_maze/cli.py` — `cmd_validate`'s new default `out_dir`
  logic, `--out-dir` help text, module docstring.
- `.gitignore` — new `pickaxe-maze-creation/{input,output}/*` ignore rules with
  `.gitkeep` exceptions.
- `~/.ssh/config` — the multi-account alias setup (`github-personal`, `github-wonderfy`,
  pinned bare `github.com`); read, not modified, this session.
- Repo remote: `git@github-personal:lunthunvuthi/AIMazeCreationStudio.git`, branch `main`.
- Run everything (unchanged from prior handoffs):
  ```bash
  source .venv/bin/activate && pytest                       # 32 backend/CLI tests
  uvicorn maze_api.main:app --reload --app-dir "Web App/backend" --port 8000
  cd "Web App/frontend" && npm run dev                       # http://localhost:5173
  cd "Web App/frontend" && npx tsc -b && npx oxlint .        # typecheck + lint
  ```

## Open questions
- Has `wf-thunvuthi-lun` accepted the collaborator invite yet? (unverified this session)
- Should `AIMazeCreationStudio` be switched to private? User was told it's currently
  public but hasn't decided.
- Which "what's next" item does the user want to pick up: Randomize-wait confirmation,
  §6.4 tutorial-slot auto-generation, Phase 2 localStorage autosave, or something else?
- Carried from `handoff_20260818_02.md`, now resolved by this session: git init (done),
  folder rename (done), validate's report location (done) — no longer open.

## Resume Prompt
```
Read handoffs/handoff-2026-08-18-1712.md to get up to speed. Last session did repo
housekeeping only (no app code): git-initialized this project (remote now
git@github-personal:lunthunvuthi/AIMazeCreationStudio.git, branch main, 2 commits, working
tree clean), renamed PickAxe Maze Creation/ to pickaxe-maze-creation/ (updated
pyproject.toml + comments, reinstalled the editable package, reverified pytest/tsc/oxlint
all clean), changed `pickaxe-maze validate` to default its report next to the input file
instead of output/, and gitignored generated CLI artifacts under
pickaxe-maze-creation/{input,output}/. Also pushed to the user's personal GitHub account
(must use the github-personal SSH alias on this machine, never bare github.com, which is
deliberately pinned to the work account's key) and invited their work GitHub account
(wf-thunvuthi-lun) as a write collaborator — that invite's acceptance is unverified, check
it first if collaboration is today's topic. The repo is currently public; user hasn't said
whether to make it private. Nothing was queued for next steps on the actual maze app itself
— options surfaced but not chosen: confirm the ~15-19s Randomize wait at star 8, build
development_plan.md §6.4 (auto-generate 1-star tutorial slot), or start Phase 2
(localStorage autosave). Dev servers (uvicorn :8000, vite :5173) were left running
throughout and were not restarted this session.
```
