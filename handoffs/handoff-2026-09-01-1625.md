# Session Handoff — 2026-09-01 (1625)

## Goal
Pick up after `handoff-2026-08-28-1628.md`. Its next step was "owner merges #9 and #10,
then start 7a" — but the owner's stated priority was **unblocking a real worksheet**, whose
blockers were all owner *decisions*. So this session collected those decisions, wrote them
into the specs, and closed **stage A1**. Owner has now chosen **Auth0 (7a) as the next
step**.

## Status snapshot
**PR #11 MERGED (`b2f02b1`) — stage A1 is closed. PR #12 is OPEN, `MERGEABLE`/`CLEAN`,
2 commits, awaiting the owner's merge.** Working tree clean on branch
`fix-pr11-review-findings`. No worksheet authoring work was done or is wanted (see
Decisions).

## Done

### PR #11 — MERGED (`b2f02b1`). A1's three questions answered, footer motif cut
Docs only, five Markdown files, zero source. `pytest -q` 32 passed.
- **Age bands → `concept.md` §2.** Ages, **not** school grades: Kinder **4–6**, Primary
  **6–9**, Advanced **9–12**. Written up as **deliberately overlapping** (they name who a
  level is *for*, not a partition) and as read by **no code** — it exists so a designer can
  judge cover wordiness. Kinder's 4-year-olds are the floor the cover is written against.
- **Exactly-zero rule stays implied → `concept.md` §5.** Recorded as a *decision*, not an
  oversight, with the cost of reversing it stated (new cover copy, a slot in
  `Front Cover.svg`, a designer pass, re-approval of a signed-off cover). The rule is
  taught by the puzzle instead — every level's opening question is fixed at 3×3 / 1 pickaxe.
- **Minimal canon → `concept.md` §1.** Pickaxe and rock only; no named setting, characters
  or world. Two consequences recorded: Advanced's owed A10 artwork is **unconstrained**, and
  the mascots stay **brand** characters.
- **Decorative footer motif → CUT** in `pdf_design_spec.md` §4/§11/§12.2. Dropped, not
  deferred.
- **`Wk` confirmed** (`pdf_export_spec.md` §3) and **Preview's `allComplete` gate kept with
  no placeholder panel** (`level_dashboard_pagination_spec.md`) — both had shipped as
  defaults and were being re-raised every handoff. Now closed.
- `PRODUCTION_PROCESS.md` §5 gap 1 → **A1 closed**.

### Code review of #11 → 4 findings, all verified real
Run as a **spec-correctness pass** (docs-only diff, so the check is whether every new claim
and cross-reference holds against the code and the other specs). I verified each finding
myself rather than taking them on faith.

### PR #12 commit 1 (`1b12bc1`) — fixed those four
- **`concept.md` §1** claimed Posuru appears on the **Kinder** last page. It doesn't.
- **`pdf_design_spec.md` §5** — #11 had wedged "it is built and it stays" into a sentence
  continuing "a renderer that omits the incorrect-example panel … is still correct", so the
  bullet argued both sides. The omit clause was sound advice *while the per-question
  renderer was unbuilt*; that window closed. Verified the panel really is built
  (`CoverPage.tsx:443-510`) before asserting it.
- **`pdf_export_spec.md` §3's cover element inventory** still listed the cut footer graphic
  with no cut note — **the worst place to miss it**, since that is the page contract someone
  implements from.
- **`pdf_design_spec.md` §1's palette** still listed `#D1D3D2` as live, its only use being
  the cut motif; the hex is nowhere in source or the designer SVGs, and the row contradicted
  §10's checklist. **Retired, not deleted**, so the measurement survives.

### PR #12 commit 2 (`c655f07`) — Posuru is NOT a Pickaxe Maze character
The important one. **The owner had already said this on 2026-08-19**
(`handoff-2026-08-19-1512.md`), and `pdf_design_spec.md` §12.1 had it right all along.
- **What went wrong:** a session building `LastPage.tsx` on 2026-08-21 looked at the
  supplied Primary JPEG, decided the large bear-like figure was Posuru, and wrote it into a
  comment. It spread to `pdf_export_spec.md` §5 and `PRODUCTION_PROCESS.md` §5 — and
  **commit 1 of this very PR propagated it into `concept.md`** while fixing a different
  error in the same sentence. The review flagged that sentence as wrong about *Kinder*
  without either the reviewer or me noticing the *name* was the real defect.
- **I opened both JPEGs** and described them from the artwork, not the prose:
  - **Kinder** — Hatenyan alone, full body, holding a pencil, "I did it!" with motion lines.
  - **Primary** (borrowed by Advanced) — **two Hatenyan flanking a third, larger bear-like
    figure**, cropped at the page's bottom edge, "Well done! You did it!".
- That third figure is now **described and deliberately NOT named** in all five sites that
  named it (`concept.md` §1, `pdf_export_spec.md` §5, `PRODUCTION_PROCESS.md` §5, two
  `LastPage.tsx` comments), **each with a note saying why** — otherwise the next reader
  compares docs to JPEG, reaches the same conclusion, and helpfully restores it.
- §12.1's row now states the rule **positively** ("this maze type uses Hatenyan and only
  Hatenyan") instead of the narrow "this vector is unused" — true, but it read as leaving
  the raster case open, which is exactly the gap the bad attribution slipped through.
  (Commit 1 had made precisely that mistake.)
- Verified: `npm run build` clean, `npx oxlint src` only the pre-existing
  `pdfMazeTypeRegistry.tsx:85` warning, `pytest -q` 32 passed. `LastPage.tsx` changes are
  **comments only**; both JPEGs untouched.

### Memory updates (outside the repo)
- New: `feedback_dont_identify_brand_assets_from_artwork.md` — describe artwork, never name
  the character; the owner is the only authority on asset usage.
- Corrected the stale `project_maze_type_production_process` memory and its `MEMORY.md`
  index line (both still said A1 had 3 open questions).

## In progress
**Nothing mid-edit. Working tree clean.** PR #12 is pushed and awaiting the owner's merge.

## Next step
1. **Owner merges [#12](https://github.com/lunthunvuthi/AIMazeCreationStudio/pull/12).**
2. **Start roadmap 7a — Auth0 / Google login.** `development_plan.md` §9 step 7a: Google
   sign-in, `users` + roles, tighten `main.py`'s `allow_origins=["*"]` (**confirmed still
   `*`** on main), authenticate the pdf-service render endpoint. ~2 days, no database.
3. **Settle the test-login story FIRST — this is the one decision that must be made at the
   start of 7a, not discovered inside it.** Google and Auth0's hosted login both block
   automated sign-in, so `scripts/phase_b_run.mjs` and `scripts/autosave_check.mjs` (29
   checks — including the **only** automated coverage of Modify Maze) both stop working the
   day auth gates the app, unless the backend gets a **test-only login path**.

## Decisions & conventions

**Do not relitigate these.**

- **BRANCH AND PR FOR EVERYTHING. Never commit or push to `main`.** (CLAUDE.md.) Followed
  all session. **Do not stack PRs** — base off `main`.
- **Owner decisions made this session** (all now written into the specs, see Done):
  age bands are **ages not grades**; cover keeps exactly-zero **implied**; canon is
  **minimal**; footer motif **cut**; `Wk` **confirmed**; Preview gate **kept**, no
  placeholder panel.
- **Posuru is NOT used by Pickaxe Maze. Hatenyan only.** The owner assigns Posuru to
  content explicitly and will say when. **Never name a brand character from artwork** —
  describe it, and ask.
- **The owner does NOT want worksheet-authoring tooling.** I proposed a "candidate contact
  sheet" (generate N valid mazes per star tier, pick the good ones); the owner declined:
  *"for the picking up the good one, it's more on human side. there is no need to make it."*
  **Do not propose it again.** Curation is human. Authoring a real sheet is content work the
  owner does, not an engineering task.
- **Storage path (2026-08-28) is Auth0 login + a deliberately tiny backend store.** NOT
  Drive-as-store, NOT a local multi-sheet library. Drive's only later role is a **one-way
  server-side mirror** of approved artifacts (roadmap step 10).
- **`sheetId` is opaque** — never display it, sort by it, or build a filename from it.
- **Approval must point at an immutable revision**, not a sheet (collab spec §3.2).
- **Do NOT answer the collaboration spec's 12 remaining questions yet.** Only **#10 ("what
  is a week?")** and **#11 (single- vs multi-tenant)** matter now.
- Carried and still deliberate: driver replays generator output click-by-click through the
  real wizard UI; driver (Node/Playwright) and verifier (Python/PyMuPDF) split; Playwright
  borrowed from `pdf-service/node_modules`; `@dnd-kit/sortable` NOT installed; solution path
  grey `#58595B`; `PRODUCTION_PROCESS.md` §5's Advanced-last-page gap stays **open**
  (artwork owed, **not** a shipping blocker — measured 2026-08-28).

## Gotchas / watch-outs

- **CHECK YOU ARE ON CURRENT `main` BEFORE READING ANY SPEC.** This session started on
  `cover-header-year`, **5 commits behind `origin/main`**, and I read a stale
  `development_plan.md` §9 (no 7a–7f) and could not find `storage_spike.md` /
  `collaboration_workflow_spec.md` **because they were not on disk**. Cost a full
  re-orientation round. `git fetch && git log --oneline -1 origin/main` first.
- **The class of bug to watch for: a plausible inference that reads as a sourced fact.**
  The Posuru attribution survived three weeks and a code review because "Posuru plus two
  Hatenyan" is indistinguishable from a real source once it is in prose. When removing one,
  leave a note at each site saying *why*, or it gets helpfully restored.
- **State asset/scope rules positively.** "This vector is unused" was true but read as
  leaving the raster case open. "This type uses Hatenyan only" closes it.
- **The app needs THREE processes:** backend `:8000`, Vite, pdf-service `:8010`.
- **`npx tsc --noEmit` in `Web App/frontend` typechecks ZERO files.** Use `npm run build`
  (+ `npx oxlint src`).
- **One pre-existing lint warning is expected:** `spike/pdfMazeTypeRegistry.tsx:85`. Not
  introduced by this session's work.
- **`localStorage` lags the store by up to 500 ms** (`WRITE_WINDOW_MS`). Reading it right
  after a store action returns the **previous** sheet.
- **`getByRole('link', {name: 'Resume'})` is ambiguous** on the maze-type home — use
  `exact: true`.
- **Randomize on the question-entry screen is a `<Link>` wrapping an `<h2>`**, not a button.
- **pdf-service defaults to `FRONTEND_URL=http://localhost:5173`.** The driver's
  `--frontend` points the driver only.
- **A stale Vite from an older session holds `:5173`** — five sessions now; use `:5174`.
- `scripts/run_backend.py` dies under `nohup … &` (watchdog, by design).
- `Playwright locator.count()` does not auto-wait; Preview's blob tab is a *download* in
  headless Chromium.
- Still **no frontend test runner**; all frontend guarantees are browser-driven.
- **`frontend_reference.md` is stale in places** (e.g. §6 describing `addQuestion(star)`) —
  left alone rather than silently widening diffs. (unverified how much else)
- **Handoffs are point-in-time records and are NOT rewritten.** Older ones still list A1's
  questions as open and still name Posuru. That is deliberate.

## Key files & locations
- [`concept.md`](../Maze-All-Contents/pickaxe-maze-creation/docs/concept.md) — **stage A1, now closed.** §1 canon + the Hatenyan-only rule, §2 age bands, §5 the cover decision, §7 the answer index.
- [`PRODUCTION_PROCESS.md`](../PRODUCTION_PROCESS.md) — canonical work order. §4 Phase B, §5 gaps (gap 1 closed).
- [`Web App/docs/development_plan.md`](../Web%20App/docs/development_plan.md) §9 — roadmap; **7a–7f are here**, plus steps 6/6.5 done and 10.
- [`Web App/docs/pdf_export_spec.md`](../Web%20App/docs/pdf_export_spec.md) §3 cover contract, §5 last page.
- [`Web App/docs/pdf_design_spec.md`](../Web%20App/docs/pdf_design_spec.md) §1 palette, §4 cut motif, §11, §12.1 assets.
- [`Web App/docs/collaboration_workflow_spec.md`](../Web%20App/docs/collaboration_workflow_spec.md) · [`storage_spike.md`](../Web%20App/docs/storage_spike.md)
- [`Web App/frontend/src/spike/LastPage.tsx`](../Web%20App/frontend/src/spike/LastPage.tsx) — per-level artwork map; comments explain the naming rule.
- [`Web App/backend/maze_api/main.py`](../Web%20App/backend/maze_api/main.py):12 — `allow_origins=["*"]`, a 7a target.
- PRs: #11 merged · [#12 OPEN](https://github.com/lunthunvuthi/AIMazeCreationStudio/pull/12)

```bash
# verify
cd "Web App/frontend" && npm run build && npx oxlint src   # tsc --noEmit proves nothing
cd ../.. && pytest -q                                      # expect 32 passed
node scripts/autosave_check.mjs                            # expect 29/29, needs Vite + backend

# run the app (three terminals)
python scripts/run_backend.py                              # :8000
cd "Web App/frontend"    && npm run dev                    # :5174
cd "Web App/pdf-service" && FRONTEND_URL=http://localhost:5174 npm start   # :8010

# full worksheet
node scripts/phase_b_run.mjs --level primary --route mixed --clean \
  --frontend http://localhost:5174
python scripts/verify_worksheet_pdf.py phase-b-out/primary-mixed
```

## Open questions

**Closed this session** (do not re-ask): `concept.md` §7's three · footer motif ·
`Wk` vs `Week` · placeholder panel · whether to build authoring/curation tooling.

**Blocked on the owner — needed for 7a/7b:**
- **Test-login path for the harness.** The one thing to decide *at the start* of 7a.
- **Collab spec #10 — what is a "week"?** ISO week, week-of-month, or term week?
  `LevelProgress` stores `week` beside `month`, implying week-of-month, and it is printed on
  every cover.
- **Collab spec #11 — one school per deployment, or multi-tenant?** Must be answered
  **before** the schema; retrofitting a tenant key touches every table and query.
- **Verify against current Auth0 docs — do NOT take from any handoff:** free-tier MAU
  allowance, and whether **Token Vault** is on the chosen plan. Do not ship with Auth0's
  **development Google keys**.

**Blocked on the owner — smaller:**
- **What is the third figure on Primary's last page?** Two Hatenyan flank a larger
  bear-like character. It is currently described and unnamed on purpose. Only the owner can
  name it; do not guess.
- **Advanced's dedicated last page** — artwork still owed. Editorial, **not** a shipping
  blocker (measured 2026-08-28).

**Repo admin (carried, unanswered):** did `wf-thunvuthi-lun` accept the collaborator
invite; should `AIMazeCreationStudio` be private?

**Not an engineering task:** there is still **no shippable worksheet**. Every Phase B run
to date has been a throwaway. The owner has explicitly said curation is human and wants no
tooling for it.

## Resume prompt
```
Read handoffs/handoff-2026-09-01-1625.md.

RULE, non-negotiable: branch and open a PR for every change. Never commit or push to
main — not even when I say "commit and push". It is in CLAUDE.md. Do NOT stack a PR on
another PR's branch; base off main.

FIRST: git fetch and confirm you are on current origin/main before reading any spec. Last
session started 5 commits behind and read a stale development_plan.md that did not contain
steps 7a-7f, and two spec files that were not on disk at all.

State: PR #11 merged — stage A1 is CLOSED (age bands 4-6/6-9/9-12, cover keeps the
exactly-zero rule implied, canon is minimal, footer motif cut, Wk confirmed, Preview's
allComplete gate kept with no placeholder panel). PR #12 is OPEN and MERGEABLE/CLEAN with
2 commits: four spec defects a review of #11 found, plus removing a wrong attribution of
the mascot Posuru to this maze type.

Decisions not to relitigate:
1. Posuru is NOT used by Pickaxe Maze — Hatenyan only. Never name a brand character by
   looking at artwork; describe it and ask me.
2. I do NOT want worksheet-authoring or maze-curation tooling. Picking good mazes is a
   human job. Do not propose it again.
3. Storage path is Auth0 login + a deliberately tiny backend store. NOT Drive-as-store,
   NOT a local multi-sheet library.
4. sheetId is opaque — never display it, sort by it, or build a filename from it.
5. Do not answer the collaboration spec's 12 remaining questions yet. Only #10 ("what is
   a week?") and #11 (single- vs multi-tenant) matter now.

Next: merge #12, then start roadmap 7a — Auth0/Google login, users + roles, tighten
main.py's allow_origins=["*"], authenticate the pdf-service render endpoint. ~2 days, no
database.

BEFORE writing any 7a code, settle the test-login question with me: Google and Auth0
hosted login block automated sign-in, so scripts/phase_b_run.mjs and
scripts/autosave_check.mjs (29 checks, the only automated coverage of Modify Maze) both
die the day auth gates the app unless the backend gets a test-only login path.

Also verify against CURRENT Auth0 docs rather than trusting the handoff: free-tier MAU
allowance, and whether Token Vault is on the plan. Do not ship Auth0's development Google
keys.
```
