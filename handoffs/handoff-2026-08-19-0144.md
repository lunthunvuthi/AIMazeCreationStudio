# Session Handoff - 2026-08-19 (0144)

## Goal
Create AI-facing implementation reference docs for the web app's frontend and backend
— separate from `development_plan.md` (the product spec) — so a future AI session can
read a doc instead of re-reading ~3000 lines of source every time. Then the user moved
both into their own subfolders and asked to fix references, hand off, and push.

## Status snapshot
Both reference docs are written, moved into subfolders per the user's request, all
cross-references fixed and verified, nothing committed yet. This handoff is the last
step before commit + push (both requested by the user, not yet done as of writing this
file).

## Done
1. Read every file in `Web App/backend/` (4 files) and `Web App/frontend/src/` (~28
   files), plus the relevant parts of `Maze-All-Contents/pickaxe-maze-creation/
   pickaxe_maze/` (function signatures only, not full algorithm) to ground the backend
   doc's description of what it imports.
2. Wrote `Web App/docs/backend_reference.md` (7 sections: what the service is, file
   map, `main.py`, the two routes, the schema/aliasing bridge, the `pickaxe_maze`
   dependency + editable-install gotcha, tests, what's not built yet).
3. Wrote `Web App/docs/frontend_reference.md` (10 sections: tech stack, file map,
   routing table, full data model walkthrough, the maze-type registry pattern +
   duplication risk between TS and Python constants, the Zustand store's every action,
   the file-based save/load adapter, page-by-page implementation quirks, a deep dive
   on `wizardMaze.ts`/`CellRenderer`/`PickaxeGrid`/the 4 wizard steps, and a gotchas
   list).
4. Added a pointer from `development_plan.md` (right after its intro, before §1)
   explaining the split: that doc is product intent, the two reference docs are
   implementation-level and should be kept in sync with code changes.
5. **User then moved both files** into new subfolders:
   `Web App/docs/backend/backend_reference.md` and
   `Web App/docs/frontend/frontend_reference.md`. Asked me to check/fix any references
   to the old flat paths.
6. Grepped the whole repo (excluding `node_modules`/`.venv`/`dist`) for
   `backend_reference.md` / `frontend_reference.md` mentions — found exactly 4, all
   inside these three docs (no code, README, or other doc referenced them). Fixed all
   4:
   - `development_plan.md` line ~15: `frontend_reference.md`/`backend_reference.md` →
     `frontend/frontend_reference.md`/`backend/backend_reference.md`.
   - `backend_reference.md` (2 mentions of `frontend_reference.md`) →
     `../frontend/frontend_reference.md`.
   - `frontend_reference.md` (1 mention of `backend_reference.md`) →
     `../backend/backend_reference.md`.
   - Re-grepped after editing to confirm all 4 now resolve correctly (see the
     grep output below, or just rerun it).

## Next step
Commit and push, as the user explicitly asked for both. Suggested commit message:
"Add frontend/backend implementation reference docs" (or similar) — covers the new
`Web App/docs/backend/backend_reference.md`, `Web App/docs/frontend/frontend_reference.md`,
and the `development_plan.md` pointer/reference-path edits. The previously-untracked
`handoffs/handoff-2026-08-19-0047.md` from the prior session is still sitting untracked
too (same pattern as every prior handoff — swept into the next commit); this session's
own handoff file will be untracked as well and should get swept in the same way.

## Decisions & conventions
- **Split established this session:** `development_plan.md` = product spec (what
  screens/endpoints should do, confirmed decisions). `backend_reference.md` /
  `frontend_reference.md` = implementation reference (what the code actually does —
  file map, data flow, gotchas). Keep the reference docs updated when code structure
  changes; keep `development_plan.md` updated when intended behavior changes. Stated
  explicitly in `development_plan.md`'s new pointer paragraph.
- **Subfolder convention (user's choice, not mine):** each reference doc now lives in
  its own subfolder under `Web App/docs/` — `backend/backend_reference.md` and
  `frontend/frontend_reference.md` — rather than flat in `docs/` alongside
  `development_plan.md`, `pdf_export_spec.md`, `level_dashboard_pagination_spec.md`.
  Cross-references between the two use `../` relative paths accordingly. If a third
  reference doc is ever added (e.g. for the `pickaxe-maze-creation` package, or the
  future PDF renderer), consider whether it should follow this same
  own-subfolder pattern for consistency.
- Both reference docs state "last verified against source: 2026-08-19" at the top —
  whoever next makes a substantive code change to frontend or backend should update
  that stamp (or the relevant section) rather than let the doc silently drift stale.

## Gotchas / watch-outs
- The reference docs' own §5 (frontend) / §6 (backend §5 area) already flag a
  **pre-existing** duplication risk, not something new: `PICKAXE_DIFFICULTY_CONFIG`
  and `PICKAXE_STAR_PARAMS` in `frontend/src/registry/` are hand-copied from
  `pickaxe_maze/difficulty.py` with no sync mechanism — if difficulty constants change,
  both places need manual updates. Flagged in the docs so a future session doesn't
  need to rediscover this by diffing the two languages.
- `[[feedback_spec_conflicts]]` (standing memory: ask before editing a design doc's
  numeric constants) — not triggered this session, no numeric constants were touched,
  only doc text and file paths.
- Don't confuse this session's doc-path fixes with the **prior** session's folder
  reorg (`project_folder_reorg` memory, `handoff-2026-08-19-0047.md`) — that one moved
  `pickaxe-maze-creation` under `Maze-All-Contents/` and created the top-level
  `docs/` subfolders per package. This session's move is one level narrower: just the
  two reference docs into their own subfolders within `Web App/docs/`.

## Key files & locations
- `Web App/docs/backend/backend_reference.md` — backend implementation reference.
- `Web App/docs/frontend/frontend_reference.md` — frontend implementation reference.
- `Web App/docs/development_plan.md` — product spec; now has a pointer to the two
  reference docs right after its intro (before §1).
- Verify no stale references remain:
  ```bash
  grep -rn "backend_reference\.md\|frontend_reference\.md" --include="*.md" --include="*.ts" --include="*.tsx" --include="*.py" . | grep -v "/node_modules/\|/.venv/\|/dist/"
  ```
  Expected: exactly 4 hits, all inside the 3 docs listed above, all using correct
  relative paths (`frontend/frontend_reference.md`, `backend/backend_reference.md`,
  `../frontend/frontend_reference.md`, `../backend/backend_reference.md`).
- Commit/push (not yet run):
  ```bash
  git add "Web App/docs/backend" "Web App/docs/frontend" "Web App/docs/development_plan.md" handoffs/
  git commit -m "..."
  git push
  ```
- Repo remote: `git@github-personal:lunthunvuthi/AIMazeCreationStudio.git`, branch
  `main`, currently at `39368bc` (this session's changes not yet committed on top).

## Open questions
- None new this session. Carried from prior handoffs, still unresolved: whether
  `wf-thunvuthi-lun` has accepted the GitHub collaborator invite, and whether
  `AIMazeCreationStudio` should be made private.

## Resume Prompt
```
Read handoffs/handoff-2026-08-19-0144.md to get up to speed. Last session wrote two
new AI-facing implementation reference docs — Web App/docs/backend/backend_reference.md
and Web App/docs/frontend/frontend_reference.md — documenting what the web app's
frontend/backend code actually does (file maps, data flow, gotchas), separate from
development_plan.md which stays the product spec. The user moved both files into their
own subfolders after they were first written flat in docs/; all 4 cross-references
between the three docs were found and fixed to use correct relative paths (verified via
grep, zero stale references remain). Nothing has been committed yet — next step is to
commit (Web App/docs/backend/, Web App/docs/frontend/, the development_plan.md pointer
edit, and this handoff) and push to origin/main, both explicitly requested by the user.
```
